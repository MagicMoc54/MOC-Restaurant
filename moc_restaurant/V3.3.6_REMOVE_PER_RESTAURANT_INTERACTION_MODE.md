# MOC Restaurant v3.3.6

Removed the per-restaurant [E] vs qb-target feature.

Removed:
- Interaction Method selection from `/moccreate`
- Edit Interaction Method from `/mocrestaurant`
- Per-restaurant `interaction_method` runtime logic
- qb-target interaction router added for this feature

Restaurant kitchen interactions return to the known working [E] prompt system.

This build is based on v3.3.5, so the confirmed Menu Management/POS single-source
fix remains intact.

Optional cleanup SQL:
`sql/upgrade_v3.3.5_to_v3.3.6.sql`

The SQL only removes the no-longer-used `interaction_method` database column.
