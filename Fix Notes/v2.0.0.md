# MOC Restaurant v2.0.0

**Release:** Business Management

## Summary

- Introduced Business Management, employees, ranks, payroll, and sales foundation.

## Included Development Notes

### V1.7.0_NOTES.md

# MOC Restaurant v1.7.0 - Trays & Bags

Cumulative on v1.6.0 and all v1.5.5 fixes.

Adds persistent package records for trays and food bags.

### V1.8.0_NOTES.md

# MOC Restaurant v1.8.0 - Drive-Thru Expansion

Carries forward per-station radii, which is especially useful for nearby
speaker/payment/pickup points.

Adds vehicle plate storage for drive-thru order matching.

### V1.5.3_DIAGNOSTIC_GUIDE.md

# MOC Restaurant v1.5.3 POS Diagnostic Build

This build adds explicit diagnostics so we can separate a NUI problem from
a restaurant/database problem.

## After restarting the resource

You should see this client notification:
`MOC Restaurant - v1.5.3 POS client loaded.`

F8 should also contain:
`[MOC Restaurant] v1.5.3 NUI client loaded.`

The server console should contain:
`[MOC Restaurant] v1.5.3 POS server diagnostics loaded.`

## Test 1 - Pure UI

Run:

`/mocuitest`

This does not use your restaurant, SQL menu, or register location.

If the custom POS appears, the NUI works.

If nothing appears, send:
- F8 output after running `/mocuitest`
- server console startup lines for `moc_restaurant`

## Test 2 - Saved register

Both commands work:

`/mocpostest`

and

`/mocpoctest`

They attempt to find the first saved Register and open its actual SQL menu.

## Test 3 - Normal register

Walk to the register and press E.

## Emergency close

`/mocposclose`

No SQL update is required.

### FIXED_BUILD_NOTES.md

# MOC Restaurant v1.5.0 Fixed Build

This rebuild corrects two problems in the earlier packages:

1. `/mocrestaurant` is now the single restaurant administration entry point.
   Use **Open Builder** inside that menu to place registers, cooking stations,
   storage, manager locations, and drive-thru points.
2. Placement now uses a dedicated in-game placement mode. Select a location
   type, walk to the exact spot, face the desired direction, press **E**, and
   confirm. Press **BACKSPACE** to cancel.
3. The FiveM manifest has been normalized to `fx_version 'cerulean'`.
   Some earlier later-version packages incorrectly contained the release number
   in the fx_version field.

Commands:
- `/moccreate` - create a restaurant.
- `/mocrestaurant` - open administration and the builder.
- `/mocorders` - available in versions that include the order system.

### V1.9.0_NOTES.md

# MOC Restaurant v1.9.0 - Animations & Immersion

Adds animation helper support for preparation and handoff actions.
No SQL schema change is required.

## Upgrade Guidance

This release should be installed over the previous MOC Restaurant version in the release sequence. Review the `sql/` folder inside the resource for any SQL upgrade file associated with v2.0.0.
